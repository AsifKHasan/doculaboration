#!/usr/bin/env python3

import textwrap

from helper.logger import *
from helper.pandoc.pandoc_writer import *
from helper.pandoc.pandoc_util import *

def generate(data, doc, section_specs, context):
    error('formatter [lot] not supported')

    section = ''

    return section
