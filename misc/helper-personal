git add -A ; git commit -m "updated" ; git push

cd /home/asifhasan/projects/asif@github.com/doculaboration/out

ODT=test
unzip ${ODT}.odt -d test-odt

pushd test-odt ; zip -r ../${ODT}.odt . ; popd

unzip XYZ.docx -d test-docx
pushd test-docx && zip -r ..\XYZ.docx . && popd
